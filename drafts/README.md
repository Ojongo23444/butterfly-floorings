# Drafts

This folder contains the latest working versions of all HTML files.
Upload these files to Claude at the start of each session instead of downloading from the live site.
After each session, replace these files with the updated versions Claude produces.
